{
  "name": "aex-extension",
  "private": true,
  "description": "Official Aex brain, tool, and environment extensions built only against public contracts",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present"
  },
  "devDependencies": {
    "@aexhq/cli": "file:../aex/packages/cli",
    "@aexhq/sdk": "file:../aex/packages/sdk"
  }
}
