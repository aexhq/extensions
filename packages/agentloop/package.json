{
  "name": "@aexhq/agentloop",
  "version": "0.2.0",
  "description": "Author custom agentloops for Brain sessions: the typed ctx surface over contracts/agentloop/v1 plus the deterministic source-bundle builder",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aexhq/extensions.git",
    "directory": "packages/agentloop"
  },
  "type": "module",
  "sideEffects": false,
  "engines": { "node": ">=22.0.0" },
  "publishConfig": { "access": "public", "provenance": true, "tag": "next" },
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" },
    "./build": { "types": "./dist/build.d.ts", "import": "./dist/build.js" },
    "./package.json": "./package.json"
  },
  "files": ["dist", "src", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.json && node --test",
    "prepack": "npm run build"
  },
  "dependencies": {
    "esbuild": "0.25.9"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
