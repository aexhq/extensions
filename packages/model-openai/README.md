# `@aexhq/model-openai`

OpenAI-compatible streaming Model component. Use `openai({ baseUrl? })` in a Brain session declaration. The session supplies its credential separately; Brain merges the immutable component configuration into the sealed provider options before execution.
